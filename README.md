# Hearten v2 - Music streaming app

### Instructions (WSL/Linux/Mac OS)
- create a python virtual env `python3 -m venv env`
- activate the env `. env/bin/activate`
- install required files
  - `pip install -r requirements.txt`
- run `python3 main.py`
